import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.shivshakti.creation',
  appName: 'shivshakti creation',
  webDir: 'out'
};

export default config;
